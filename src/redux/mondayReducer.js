let initialState = {
    arraySkills:
        [
            {skill: "рационалист"},
            {skill: "комуннист"},
            {skill: "анимешник"}
        ],
    listOfNames: [
        // {name: "Alla"},
        // {name: "Sasha"}
    ]

}

let mondayReducer = (state = initialState, action) => {
    return state
}

export default mondayReducer;